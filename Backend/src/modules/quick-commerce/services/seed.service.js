/**
 * Quick Commerce Seeding Service
 * NOTE: Automatic seeding is permanently disabled as per project requirements.
 * Data must only originate from real admin and seller creation.
 */

export const ensureQuickCommerceSeedData = async () => {
  // No-op: automatic seeding is disabled
  return Promise.resolve();
};

export default {
  ensureQuickCommerceSeedData,
};
